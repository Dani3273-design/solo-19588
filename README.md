# 24点游戏

使用 Python + Pygame 开发的24点纸牌游戏。

## 游戏规则

1. 系统随机发放4张扑克牌
2. 玩家需要使用这4张牌的数字，通过加、减、乘、除和括号组成算式
3. 每个数字只能使用一次，最终结果必须等于24
4. A = 1, J = 11, Q = 12, K = 13

## 功能特性

- **计时功能**：实时显示游戏用时
- **放弃功能**：游戏过程中可随时放弃，查看正确答案
- **提交记录**：记录尝试次数（排除非法字符的尝试）和最近3次提交的算式
- **结算页面**：
  - 成功：显示你的算式、用时、尝试次数和最近提交
  - 失败：显示正确答案、用时、尝试次数和最近提交

## 目录结构

```
main/
├── game/
│   ├── __init__.py    # 包初始化
│   ├── card.py        # 扑克牌相关代码（Card类、发牌逻辑）
│   ├── check.py       # 算式校验逻辑和正确答案求解
│   ├── count.py       # 计时器和提交记录统计
│   └── ui.py          # Pygame 界面与游戏流程
├── main.py            # 游戏入口
├── requirements.txt   # 依赖包
└── README.md          # 项目说明
```

## 模块说明

### [card.py](game/card.py)
- `Card` 类：表示单张扑克牌，包含花色和数字
- `generate_deck()`：生成一副52张的扑克牌
- `draw_unique_cards(count=4)`：随机抽取不重复的扑克牌（保证有解）

### [check.py](game/check.py)
- `validate_expression(expression, card_values)`：校验算式
  - 检查非法字符
  - 检查数字是否与卡牌一致且只使用一次
  - 计算结果是否等于24
- `find_correct_answer(card_values)`：求解24点的正确答案
  - 使用递归回溯算法找出一个可行的算式

### [count.py](game/count.py)
- `Timer` 类：多线程计时器
  - 独立线程运行，不阻塞UI
  - 支持开始、暂停、继续、停止
  - 格式化时间显示
- `SubmissionRecord` 类：提交记录统计
  - 记录有效尝试次数（排除非法字符的尝试）
  - 保存最近3次提交的算式
  - 支持重置统计数据

### [ui.py](game/ui.py)
- `GameUI` 类：游戏主界面
  - 菜单页面：游戏标题、说明、开始按钮
  - 游戏页面：4张扑克牌、输入框、提交按钮、放弃按钮、计时器
  - 成功结算页面：卡牌展示、算式、用时、尝试次数、最近提交、返回按钮
  - 失败结算页面：卡牌展示、正确答案、用时、尝试次数、最近提交、返回按钮
- `Button` 类：通用按钮组件
- `InputBox` 类：输入框组件
- `draw_card()`：绘制扑克牌

### [main.py](main.py)
- 游戏入口，创建并运行 `GameUI`

## 运行方式

```bash
cd main
pip install -r requirements.txt
python main.py
```

## 操作说明

- 输入框：直接键盘输入算式
- 提交答案：点击"提交答案"按钮或按回车键
- 放弃游戏：点击"放弃"按钮，跳转到失败结算页面查看正确答案
- 退出游戏：按 ESC 键或关闭窗口
